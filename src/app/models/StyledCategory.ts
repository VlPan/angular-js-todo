export class StyledCategory {
    name: string;
    icon: string;
    modificator: string;

    constructor(name:string, icon:string , modificator:string){
        this.name = name;
        this.icon = icon;
        this.modificator = modificator;
    }
}
